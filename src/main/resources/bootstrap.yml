# 注册中心地址
eureka:
  client:
    service-url:
      defaultZone: http://open.fehead.com:8761/eureka

spring:
  application:
    name: fehead-ca
  # 加密配置文件
  profiles:
    active: enc
  cloud:
    # 配置中心相关配置
    config:
      discovery:
        enabled: true
        service-id: FEHEAD-CONFIG-SERVER
      profile: test
